using namespace std;
using ll=long long;
using ull=unsigned long long;

#include <bits/stdc++.h>
#include <iostream>
#include <string>
#include<algorithm>

int main(){
    std::cin.tie(nullptr);
    cin.tie(0);

    int a, b, c;
    string s;
    cin >> a >> b >> c >> s;
    cout << (a + b + c) << ' ' << s << endl;

    return 0;
}